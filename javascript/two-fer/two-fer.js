export const twoFer = (name) => {
    //if statment
if (name) {
    return 'One for ' + name + ', one for me.'
    //if a name is given then return the above
} 
else
//if there is not name given then return the below
    return 'One for you, one for me.'


}